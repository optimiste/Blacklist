{% extends 'base.html.twig' %}

{% block body %}
    <h1>Varatotojo kūrimas</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('users_index') }}">Grįžti į sąrašą</a>
        </li>
    </ul>
{% endblock %}
