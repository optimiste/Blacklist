{% extends 'base.html.twig' %}

{% block body %}
    <h1>Varatotojo kūrimas</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Sukurti" class = "btn btn-success"/>
    {{ form_end(form) }}
            <a href="{{ path('users_index') }}" class = "btn btn-info">Grįžti į sąrašą</a>
{% endblock %}
